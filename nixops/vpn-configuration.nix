{
  # List the clients secrets here
  clients = {
    a = {
      addresses = [ "" ];
      privateKey = "";
    };
  };

  # Configuration of the VPN server
  server = {
    publicKey = "";
    allowedIPs = [ "" ];
    ip = "";
    port = 0;
  };
}
